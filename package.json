{
  "name": "tablero-jira",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tablero de sprint para el proyecto FutBot (Jira ING)",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "vercel-dev": "vercel dev"
  },
  "engines": {
    "node": ">=20.6"
  }
}
